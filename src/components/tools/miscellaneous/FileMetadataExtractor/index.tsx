import styles from './index.module.css'

export default function FileMetadataExtractor() {
    return (
        <div className={`tool-container ${styles['container']}`}>
            <h1 className="tool-title">File Metadata Extractor</h1>
        </div>
    )
}
